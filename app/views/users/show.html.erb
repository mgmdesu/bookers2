<div>
  <%= flash[:notice] %>
</div>
<!--User_infoのページ-->
<%= render 'books/user_info', book: @book, user: @user %>
<!--マイページ(Userページ)-->
<h2><strong>Books</strong></h2>

<table>
  <tr>
    <th>Title</th>
    <th>Opinion</th>
  </tr>

  <% @books.each do |book| %>
  <tr>
    <td><%= book.title %></td>
    <td><%= book.body %></td>
  </tr>
  <% end %>
</table>

